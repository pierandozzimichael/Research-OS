# Start here — Research OS demo

This download is a synthetic onboarding workspace. Read `AGENTS.md`, then
resolve the active project in `projects.json` and read its dashboard. Markdown
records are canonical; generated navigation is disposable.

For the fastest tour, open the demo paper and follow its typed path to the
claim, hypothesis, experiment, and result. Do not cite demo records as science.
